import {fail,ok} from "@/lib/api/response";import {checkRateLimit} from "@/lib/auth/rate-limit";import {verifyEmployeeAndCreateSession} from "@/lib/auth/mobile-auth-service";import {employeeVerifySchema} from "@/lib/validation/auth";
export async function POST(request:Request){const body=employeeVerifySchema.safeParse(await request.json().catch(()=>({})));if(!body.success)return fail("invalid_input","ข้อมูลพนักงานไม่ถูกต้อง",422);if(!checkRateLimit(`employee:${body.data.contextId}:${body.data.employeeCode}`))return fail("rate_limited","ลองใหม่อีกครั้งภายหลัง",429);const result=await verifyEmployeeAndCreateSession(body.data.contextId,body.data.employeeCode,body.data.pin);if(!result)return fail("invalid_credentials","รหัสพนักงานหรือ PIN ไม่ถูกต้อง",401);return ok({...result,redirectTo:"/dashboard"})}
